import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  sliderLabel: {
    position: "absolute",
    top: -5,
    width: 20,
  },
  sliderLabelText: {
    // alignItems: 'center',
    textAlign: "center",
    fontSize: 12,
  },
  markerPressed: {
    fontWeight: "bold",
  },
});

export default styles;
